package com.parch.combine.core.component.settings.config;

public enum FieldTypeEnum {
    ID, TEXT, EXPRESSION, SELECT, BOOLEAN, NUMBER, MAP, CONFIG, OBJECT, ANY, COMPONENT,
    ELEMENT, TRIGGER, DATA_LOAD;
}
