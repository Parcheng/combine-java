{
  "constant": {},
  "init": [],
  "flows": {
    "db/add": [
      {
        "id": "addData",
        "type": "access.mysql",
        "sqlType": "INSERT",
        "sql": "INSERT INTO `t_component_test`(`id`, `name`, `size`, `data`, `create_time`, `update_time`, `delete_flag`) VALUES (${id}, ${name}, ${size}, ${data}, NOW(), NOW(), 0);"
      }
    ],
    "db/update": [
      {
        "id": "updateData",
        "type": "access.mysql",
        "sqlType": "UPDATE",
        "sqlConfigs": [
          { "sql": "UPDATE `t_component_test` SET " },
          { "conditions": ["#{name} NO_EMPTY"], "sql": "`name` = ${name}, "},
          { "conditions": ["#{size} NO_EMPTY"], "sql": "`size` = ${size}, "},
          { "conditions": ["#{data} NO_EMPTY"], "sql": "`data` = ${data}, "},
          { "sql": "`update_time` = NOW() WHERE `id` = ${id};"}
        ]
      }, {
        "id": "queryOneData",
        "type": "access.mysql",
        "sqlType": "SELECT_ONE",
        "sql": "select * from t_component_test where id = ${id};"
      }
    ],
    "db/delete": [
      {
        "id": "deleteData",
        "type": "access.mysql",
        "sqlType": "DELETE",
        "sql": "delete from `t_component_test` where id = ${id};"
      }
    ],
    "db/list": [
      {
        "id": "queryData",
        "type": "access.mysql",
        "sqlType": "SELECT",
        "sql": "select * from t_component_test;"
      }
    ],
    "db/page": [
      {
        "id": "pageData",
        "type": "access.mysql",
        "sqlType": "SELECT_LIMIT",
        "sql": "select * from t_component_test;"
      }
    ],
    "db/condition": [
      {
        "id": "conditionData",
        "type": "access.mysql",
        "sqlType": "SELECT_ONE",
        "sqlConfigs": [
          { "sql": "select * from t_component_test where 1=1" },
          { "conditions": ["#{id} NO_EMPTY"], "sql": "and `id` = ${id} "},
          { "conditions": ["#{size} NO_EMPTY"], "sql": "and `size` = ${size} "}
        ]
      }
    ]
  }
}